package 초급.DFS;

public class 개념 {
    /**
     * DFS(Depth First Search)
     * 깊이 우선 탐색
     * 최대한 밑으로 가본 뒤, 없으면 옆으로 간다.
     */
    class DFS {
    }

    /**
     * 개념(Breadth First Search)
     * 너비 우선 탐색
     * 최대한 옆으로 가본 뒤, 없으면 밑으로 간다.
     */
    class BFS {
    }
}
